# if Statements

Rust's if statements follow the subsequent syntax:
``` rust
if logical check {
    // block of code
} else if second logical check{
    // block of code
} else {
    // block of code
}
```